# ZaZet Solutions — repo notes

This repo is the **ZaZet Solutions** business site.

- **Deploys to:** `zazet-solutions.hr` via GitHub Pages (`main` branch, root, served from `index.html`, CNAME `zazet-solutions.hr`).
- **GitHub:** `tatarco/zazet-solutions`
- **Business WhatsApp:** `+385991904995` (Croatian number). All `wa.me/` links here use it.

## Sibling repo — the personal site

The personal portfolio **`gal.tidhar.org.il` is a SEPARATE repo: `tatarco/gal-tidhar`** (its own GitHub Pages deploy, served from that repo's `index.html`, CNAME `gal.tidhar.org.il`). The two sites are often worked on in parallel.

- That site's WhatsApp is the **personal** number `+972503843427` — different from this site's business number.
- To change the personal site, edit `tatarco/gal-tidhar/index.html` and push there. Changes here do **not** affect it.

⚠️ **Decoy file:** `_gal_tidhar_index.html` in this repo's working dir is a **stale, untracked copy** of the personal site and lags behind the real one. Never edit or deploy it — always use the `tatarco/gal-tidhar` repo.
